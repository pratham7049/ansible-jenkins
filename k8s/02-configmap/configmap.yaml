apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-app-config
  namespace: demo-app
  labels:
    app: demo-app
data:
  APP_ENV: "production"
  APP_LOG_LEVEL: "info"
  FEATURE_TOGGLE_V2: "true"
  app-config.json: |
    {
      "site_title": "EKS Learning Platform",
      "max_connections": 500,
      "cache_enabled": true
    }
